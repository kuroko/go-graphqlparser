# Binaries for programs and plugins
*.exe
*.dll
*.so
*.dylib

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out
*.svg

lexer/cmd/cmd
lexer/out/out
lab/block/block
lab/generics/generics

# Third-party dependencies
node_modules/
vendor/
