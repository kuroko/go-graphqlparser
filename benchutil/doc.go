// Package benchutil contains helpers for lexer benchmarks. In some cases we have to copy portions
// of libraries into our repository to be able to use them (e.g. if their lexer is in an internal
// folder).
package benchutil
